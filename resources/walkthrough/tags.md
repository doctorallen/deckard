## Tags are the connections

- `#follow-up` — a plain tag.
- `#project/atlas` — a namespace and a value, which groups a project.
- `@dana` — a person.

A tag on a heading covers the section under it; a tag on a task is that
task's own. Every tag has a page listing everything that carries it.

## Two ways to search

- **Find** — results as you type, titles first.
- **A search page** — the whole result, with **Refine** beside it.

Searches are written in plain terms: words, `#tags`, `is:open`, `has:due`,
`in:folder`, `updated >= 7d`, joined with `AND`, `OR`, and `NOT`.

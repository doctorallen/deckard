# Ghostline relay relevance samples

Use these separate notes as Related Notes candidates while selecting a tagged
entry in [[Tag Scenarios]]. The headings intentionally isolate direct and
ancestor-only matches so their relevance order is visible.

## Direct follow-up #project-name #follow-up

- [ ] Send the review outcome to the relay crew #project-name #follow-up

This should be among the strongest results for a selected
`#project-name #follow-up` task because both tags are written directly on the
entry.

## Project-only relay work #project/ghostline-relay

- [ ] Compare the relay handoff against the field log #project/ghostline-relay

This is discoverable through the selected task's Ghostline relay parent
context, but should rank below a direct follow-up match.

## Organization-only planning #org/echo-foundry

- [ ] Ask Echo Foundry for the deployment window #org/echo-foundry

This tests the more distant organization context from the tagged parent
heading.

## Management-only review #management/performance

- [ ] Publish the performance review criteria #management/performance

This tests nearest-parent management context without sharing either direct
task tag.

## 2026-09-10 field log #project/name #checkin

This broad date-style heading deliberately carries the same two tags as its
specific child.

### Ghostline deployment check-in #project/name #checkin

- [ ] Confirm the relay crew arrived for the check-in #project/name #checkin

When Related Notes is scoped to another `#project/name #checkin` entry, this
child should appear above the date heading. The child keeps its full relevance
score, while the broader date heading shows the small specificity adjustment
in its relevance explanation.

## Unrelated control #topic/quantum-drift

- [ ] Calibrate the drift sensor #topic/quantum-drift

This should not appear for the selected follow-up task unless another evidence
source, such as its own explicit tag association, connects it.

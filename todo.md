# Fixes
- tag multi-select on task dashboard needs style fixes

# Improvements
- sidebar needs UX review
- need to do a style consistency pass, there are small inconsistencies all over
- when looking at related notes, the parent tags should also be considered relevant but with less weight. For example if looking at a line that specifically mentions a person but that is under a heading for a specific project, that project is still relevant

# Themes
interstellar - called "cooper"
